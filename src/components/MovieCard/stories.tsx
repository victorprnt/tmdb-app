import { Story, Meta } from '@storybook/react'
import MovieCard from '.'

export default {
  title: 'MovieCard',
  component: MovieCard
} as Meta

// export const Default: Story = () => <MovieCard />
